.fen-element-wrapper(class='{{elementClass}}' ng-class='{"{{elementClass}}-hover":isInfoShown}')
  .fen-element
    .vertical-alignment
      span {{title }}
  .fen-element-cover(ng-show='isInfoShown').slide-up.ng-hide
    .vertical-alignment
      a(ui-sref='app.{{elementClass}}-personality')
        span.info Больше
